using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewHandler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        TimeHandler.DateTimeStamp currentTime = new TimeHandler.DateTimeStamp("2021-07-22");
        TimeHandler.DateTimeStamp targetTime = new TimeHandler.DateTimeStamp("2021-07-23");

        

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
